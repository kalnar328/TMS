//contains database credentials
var credentials = {
    database: {
         HOST : 'localhost',
         USER  :'root',
         PASSWORD :'',
         DATABASE : 'tms'
    }
}

module.exports = credentials;